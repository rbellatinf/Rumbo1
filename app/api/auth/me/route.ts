import { NextRequest } from "next/server";
import { accessConfiguration, backendMessage, noStoreJson, parseJson, providerHeaders, providerUrl, RUMBO_SESSION_COOKIE } from "../../../../lib/rumbo-access";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const provider=accessConfiguration();if(!provider)return noStoreJson({message:"El acceso todavía no está configurado."},503);const token=request.cookies.get(RUMBO_SESSION_COOKIE)?.value;if(!token)return noStoreJson({message:"No hay una sesión activa."},401);const upstream=await fetch(providerUrl(provider,"/api/access/me"),{headers:providerHeaders(provider,{token}),cache:"no-store"});const payload=await parseJson(upstream);if(!upstream.ok){const response=noStoreJson({message:backendMessage(payload,"La sesión venció o no es válida.")},upstream.status||401);response.cookies.delete(RUMBO_SESSION_COOKIE);return response}return noStoreJson({account:payload.account,profile:payload.profile,redirectTo:payload.redirect_to})}
