"use client";
import { CreditCard, Headphones, ShieldCheck } from "lucide-react";
import { useEffect, useState } from "react";
import type { TravelPackage } from "../../lib/travel-packages";
import BookingModal from "./BookingModal";
import OffersGrid from "./OffersGrid";
import SearchPanel from "./SearchPanel";
import type { SourceState } from "./AirportField";

function dateFromNow(days:number){const value=new Date();value.setDate(value.getDate()+days);return value.toISOString().slice(0,10)}
export default function NativeHome(){const[origin,setOrigin]=useState("Lima (LIM)"),[originCode,setOriginCode]=useState("LIM"),[destination,setDestination]=useState("Cusco (CUZ)"),[destinationCode,setDestinationCode]=useState("CUZ"),[departure,setDeparture]=useState(()=>dateFromNow(45)),[returnDate,setReturnDate]=useState(()=>dateFromNow(52)),[deals,setDeals]=useState<TravelPackage[]>([]),[state,setState]=useState<SourceState>("loading"),[airportState,setAirportState]=useState<SourceState>("idle"),[provider,setProvider]=useState(""),[message,setMessage]=useState("Consultando el catálogo nativo de Rumbo…"),[error,setError]=useState(""),[searching,setSearching]=useState(false),[selected,setSelected]=useState<TravelPackage|null>(null),[searched,setSearched]=useState(false);useEffect(()=>{let active=true;fetch("/api/catalog",{cache:"no-store"}).then(async response=>{const body=await response.json() as {packages?:TravelPackage[];message?:string};if(!response.ok)throw new Error(body.message||`Catálogo respondió HTTP ${response.status}`);return body}).then(body=>{if(!active)return;setDeals(Array.isArray(body.packages)?body.packages:[]);setState("live");setProvider("Rumbo");setMessage(body.message||"Catálogo nativo conectado.")}).catch(reason=>{if(!active)return;setDeals([]);setState("error");setMessage(reason instanceof Error?reason.message:"No pudimos consultar el catálogo.")});return()=>{active=false}},[]);const swap=()=>{setOrigin(destination);setOriginCode(destinationCode);setDestination(origin);setDestinationCode(originCode)};async function search(){if(!originCode||!destinationCode){setError("Selecciona un aeropuerto de la lista para origen y destino.");return}if(Date.parse(returnDate)<=Date.parse(departure)){setError("La fecha de regreso debe ser posterior a la salida.");return}setSearching(true);setState("loading");setError("");try{const query=new URLSearchParams({origin:originCode,destination:destinationCode,destinationName:destination.replace(/\s*\([A-Z]{3}\).*$/i,""),departureDate:departure,returnDate,adults:"2"}),response=await fetch(`/api/packages?${query}`,{cache:"no-store"}),body=await response.json() as {provider?:string;packages?:TravelPackage[];message?:string};if(!response.ok)throw new Error(body.message||`Búsqueda respondió HTTP ${response.status}`);setDeals(Array.isArray(body.packages)?body.packages:[]);setProvider(body.provider||"Rumbo");setMessage(body.message||"Búsqueda completada.");setState("live");setSearched(true);window.setTimeout(()=>document.getElementById("ofertas")?.scrollIntoView({behavior:"smooth"}),50)}catch(reason){const text=reason instanceof Error?reason.message:"No pudimos completar la búsqueda.";setDeals([]);setMessage(text);setError(text);setState("error")}finally{setSearching(false)}}return <main><section className="hero"><div className="hero-overlay"/><header className="site-header"><a className="brand" href="#">rumbo<span>.</span></a><nav className="desktop-nav"><a href="#ofertas">Paquetes</a><a href="/reservas">Mis reservas</a><a href="/acceso/partner">Partners</a><a href="/acceso/minorista">Agencias</a></nav></header><div className="hero-content"><div className="hero-copy"><p className="eyebrow">Viaja desde Perú</p><h1>Tú decides el rumbo.</h1><p>Encuentra vuelos, hoteles y paquetes para descubrir destinos inolvidables, dentro y fuera del país.</p></div><SearchPanel origin={origin} destination={destination} departureDate={departure} returnDate={returnDate} travellers={2} searching={searching} airportState={airportState} packageState={state} packageProvider={provider} error={error} onOrigin={(value,code)=>{setOrigin(value);setOriginCode(code)}} onDestination={(value,code)=>{setDestination(value);setDestinationCode(code)}} onSwap={swap} onDeparture={setDeparture} onReturn={setReturnDate} onAirportState={setAirportState} onSearch={search}/><div className="trust-row"><div className="trust-item"><ShieldCheck/><div><strong>Reserva segura</strong><span>Cupos validados en Rumbo</span></div></div><div className="trust-item"><CreditCard/><div><strong>Pago seguro</strong><span>Izipay desde Rumbo API</span></div></div><div className="trust-item"><Headphones/><div><strong>Soporte local</strong><span>Antes, durante y después</span></div></div></div></div></section><OffersGrid deals={deals} state={state} message={message} heading={searched?`Opciones para ${destination}`:"Escapadas que marcan el rumbo"} onOpen={setSelected}/><footer className="site-footer"><a className="brand footer-brand" href="#">rumbo<span>.</span></a><p>Viajes simples, experiencias enormes.</p><nav><a href="/reservas">Consultar reserva</a><a href="/panel">Portal Partner</a><a href="/agencia">Portal Agencia</a></nav><small>© 2026 Rumbo.</small></footer>{selected?<BookingModal deal={selected} origin={originCode} destination={destinationCode} departure={departure} returnDate={returnDate} onClose={()=>setSelected(null)}/>:null}</main>}
